[package]
name = "chaos_map"
version = "0.1.0"
edition = "2021"

[dependencies]
ctru = { git = "https://github.com/devkitPro/ctru-rs.git" }
libm = "0.2"

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
